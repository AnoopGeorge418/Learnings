/**
 * Primative Datatypes - it can also converted into non primative using "new" keyword --- datatype which does not have objects and its methods
 * String
 * Number
 * Boolean
 * Symbol
 * null
 * undefined
 * BigInt
 */

let balance = 120;
let anotherBalance = new Number()

// console.log(typeof balance); // checking the datat type of a variable

// console.log (balance);
// console.log (anotherBalance);

// console.log (typeof anotherBalance);

// null(empty) & undefined(no values asigned)
let firstname; 
// console.log(firstname); 


